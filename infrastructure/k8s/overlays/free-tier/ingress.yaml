apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: atlaspay-ingress
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: atlaspay-app
            port:
              number: 8080
