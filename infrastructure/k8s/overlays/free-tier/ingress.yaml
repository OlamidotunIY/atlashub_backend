apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: atlashub-ingress
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: atlashub-app
            port:
              number: 8080
